export const setModalText = (status) => ({
  type: "SET_MODAL_TEXT",
  payload: status,
});

export const setModalVisibility = (status) => ({
    type: "SET_MODAL_VISIBILITY",
    payload: status,
  });
  
