import React from 'react';

import Print from '../../../features/print';

function DScreen() {
  return (
    <div>
      <Print wc={'H-SS SEMI'} title={'H-SS SEMI'} type={'print'}/>
    </div>
  );
}

export default DScreen;
