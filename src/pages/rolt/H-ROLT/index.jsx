import React from 'react';

import Print from '../../../features/print';

function DScreen() {
  return (
    <div>
      <Print wc={'H-ROLT'} title={'H-ROLT'} type={'rolt'}/>
    </div>
  );
}

export default DScreen;
