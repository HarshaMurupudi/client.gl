import React from 'react';

import Print from '../../../features/print';

function DScreen() {
  return (
    <div>
      <Print wc={'L-REWIND'} title={'L-REWIND'} type={'converting'}/>
    </div>
  );
}

export default DScreen;
