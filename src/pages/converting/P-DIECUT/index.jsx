import React from 'react';

import Print from '../../../features/print';

function DScreen() {
  return (
    <div>
      <Print wc={'P-DIECUT'} title={'P-DIECUT'} type={'converting'}/>
    </div>
  );
}

export default DScreen;
