import React from 'react';

import Print from '../../../features/print';

function DScreen() {
  return (
    <div>
      <Print wc={'V-FINISH'} title={'V-FINISH'} type={'finishing'}/>
    </div>
  );
}

export default DScreen;
