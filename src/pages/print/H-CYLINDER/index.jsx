import React from 'react';

import Print from '../../../features/print';

function DScreen() {
  return (
    <div>
      <Print wc={'H-CYLINDER'} title={'H-CYLINDER'} type={'print'}/>
    </div>
  );
}

export default DScreen;
